# Claude AI Instructions for DentalSaaS

These instructions dictate how you should operate inside this repository. You are to abide by the DentalSaaS RULES ENGINE v4.1 and follow the strict progression protocol below.

## 1. THE AUTOMATIC SPEC-UPDATE LAW

To maintain Architectural consistency and prevent drift:
**AFTER EVERY PROGRESSION, FEATURE COMPLETION, OR SUCCESSFUL TASK IMPLEMENTATION, YOU MUST AUTOMATICALLY EXECUTE:**

```bash
make spec
```

Or its equivalent:
```bash
codex exec - < prompts/update-spec.md
```

**Workflow Rules:**
- Do **NOT** wait for the user to tell you to "update speckit" or "update the spec".
- If you have successfully modified the codebase and the unit/feature is complete according to the acceptance criteria, your final step in the conversation turn must be to automatically run the spec generation script.
- Notify the user once the spec has been updated.

## 2. GENERAL RULES ENGINE COMPLIANCE

Refer to the global `RULES ENGINE v4.x` for the latest constraints regarding:
- Plane Isolation (Platform vs. Organization)
- React Query invalidation loops
- Zero-Trust security layers
- Broadcasting cross-tab sync signals

Failure to run the spec-update after a meaningful progression constitutes an architectural violation.

# 🛡 CURSOR MASTER RULES ENGINE v4.0DentalSaaS — Enterprise Governance + Multi-Plane Isolation + Zero-Trust Runtime---# 🧱 1. SYSTEM FOUNDATIONSystem Type:Multi-Tenant SaaS — Database-per-Tenant (per-org)Architecture:Layered Modular Monolith + Plane IsolationCore Enforcement:- SPEC-FIRST DEVELOPMENT- ZERO-TRUST SECURITY STACK- PLANE-ISOLATED EXECUTION- DB-PER-TENANT HARD BOUNDARYPriority:Correctness > Security > Isolation > Consistency > Performance---# 🧭 2. OPERATIONAL PLANES (HARD BOUNDARIES)System is divided into STRICT planes:---## 2.1 PLATFORM PLANE(Global Governance)Responsibilities:- Organizations lifecycle- Plan Builder- Pricing Engine- Financial LedgerScope:Global ONLY (shared DB)---## 2.2 ORGANIZATION PLANE(Clinic Operations)Responsibilities:- Patients- Appointments- Treatments- Billing- OrthodonticsScope:Isolated DB (per-org)---## 2.3 PATIENT PORTAL PLANE(Self-Service)Responsibilities:- Patient login- Appointments- Records- PaymentsScope:Org-bound (restricted access)---## 2.4 SUPERVISOR PLANE(Multi-Clinic Oversight)Responsibilities:- Aggregated analytics- Cross-org reportingScope:Controlled cross-org read ONLY---# 🚨 3. PLANE ISOLATION LAW (ABSOLUTE)Cursor MUST enforce:---## ❌ FORBIDDEN- Cross-plane DB queries- Importing models from another plane- Mixing business logic across planes- Direct access between planes---## ✅ ALLOWEDONLY via:- Service Layer APIs- Explicit interface contracts---## 🚨 VIOLATION RESPONSE❌ BLOCKED — PLANE ISOLATION VIOLATIONPlane: [source plane]Illegal Access: [target plane]Fix: Use service/API boundary---# 🏢 4. TENANT ISOLATION (DB-PER-ORG)---## RULES- Each org = isolated database- NO organizationId enforcement inside org DB- NO cross-database queries---## SHARED DB (Platform Plane)Contains:- Organizations- Plans- Platform Users---## 🚨 VIOLATIONS- Mixing shared + org data directly- Injecting organizationId in per-org mode→ BLOCK---# 🔐 5. 7-LAYER ZERO-TRUST SECURITY STACK (MANDATORY)Cursor MUST enforce ALL layers:---## 5.1 CONTEXT LAYER (AUTH)- JWT separation:  - PLATFORM JWT  - ORG JWT- MUST validate before ANY logic---## 5.2 LICENSE LAYER (ENTITLEMENTS)- MUST call:requireEntitlement(module)- Block access if plan invalid---## 5.3 AUTHORITY LAYER (RBAC)- MUST check permission:permissionSet.has('patients.create')---## 5.4 CONTEXTUAL LAYER (PBAC)- MUST enforce:policyMiddleware- Ownership / branch constraints REQUIRED---## 5.5 ISOLATION LAYER (RLS)- secureModel MUST be used- Auto-scoping REQUIRED---## 5.6 VISIBILITY LAYER (FLS)- MUST filter sensitive fields:  - financial  - PII---## 5.7 AUDIT LAYER- ALL mutations MUST log:  - hash-chained (SHA-256)  - immutable---## 🚨 SECURITY VIOLATIONIf ANY layer missing:❌ BLOCKED — ZERO-TRUST VIOLATIONMissing Layer: [Layer Name]Fix: Apply required middleware---# ⚙️ 6. DEVELOPMENT GOVERNANCE---## 6.1 SPEC-FIRST MODEL (MANDATORY)Flow:spec.md ↓ plan.md ↓ tasks.md ↓ implementationNO skipping allowed---## 6.2 SOVEREIGN GUARDSCursor MUST respect:- validateSecurityModes- validateAuthPipelineIf config unsafe:→ REFUSE generation---## 6.3 DRIFT DETECTION AWARENESSCursor MUST NOT:- Introduce ghost permissions- Bypass RLS- Create untracked fields---# 🧠 7. BACKEND RULES- MUST belong to ONE plane only- MUST use service layer- MUST include:  - validation  - RBAC + PBAC  - error handling  - audit logging---# 🎨 8. FRONTEND RULES---## Capability-Based UI ONLY❌ Forbidden:role === 'admin'✅ Required:useCapability('patients.create')---## Plane IsolationDirectories MUST remain separated:/src/platform /src/org /src/patient /src/supervisorNO cross-imports---## State Management- Zustand → global UI state- React Query → server state ONLY---# 🤖 9. AI ENGINE RULES- Runs as isolated Python service- NO direct DB access- ONLY via API or queue (BullMQ)---# 🧾 10. COMMERCIAL SYSTEM RULES---## Billing OrchestratorPattern:Activate → THEN Invoice---## Plan Builder- Immutable PlanVersion- NO mutation after activation---## Usage Tracking- MUST use atomic counters- NO derived calculations---# 🚨 11. REACT QUERY ENFORCEMENT (CRITICAL)Cursor MUST enforce all sub-rules below. Violations are HARD BLOCKS.---## RULE 11.1 — SERVER STATE LAWALL server data MUST come from `useQuery`.`useState` for server data is FORBIDDEN.❌ Forbidden:```javascriptsetPlans(apiResponse.data)setTemplates(res.data)```✅ Required:```javascriptuseQuery({ queryKey: PLAN_QUERY_KEYS.x, queryFn: () => api.get(...) })```---## RULE 11.2 — QUERY KEY REGISTRY LAWALL queries MUST use centralized `PLAN_QUERY_KEYS`.Hardcoded strings are FORBIDDEN.Registry: `@/lib/query/planQueryKeys.js`❌ Forbidden:```javascriptuseQuery({ queryKey: ["plans"] })useQuery({ queryKey: ["publicPlans"] })```✅ Required:```javascriptuseQuery({ queryKey: PLAN_QUERY_KEYS.publicPlans(country) })```---## RULE 11.3 — CROSS-PLANE INVALIDATION (MANDATORY)Platform mutations MUST invalidate `PLAN_QUERY_KEYS.ALL_PUBLIC`.Required after:- save draft- publish- visibility change- duplicate- deprecate❌ BLOCK IF MISSING:```javascriptqueryClient.invalidateQueries({ queryKey: PLAN_QUERY_KEYS.ALL_PUBLIC });```---## RULE 11.4 — NO MANUAL REFETCH❌ Forbidden:```javascriptrefetch()window.location.reload()window.location.href = ...```✅ Required:```javascriptqueryClient.invalidateQueries({ queryKey: PLAN_QUERY_KEYS.x })```---## RULE 11.5 — SINGLE QUERY CLIENTALL planes MUST use the shared instance:`@/lib/query/queryClient.js`❌ Forbidden:```javascriptnew QueryClient()   // inside any component or module```---## RULE 11.6 — PERFORMANCE STANDARDS| Query Type | staleTime | gcTime ||-----------|-----------|--------|| Public pricing | 60s | 5 min || Platform lists | 30s | 5 min || Single version | 30s | 5 min |`refetchOnWindowFocus: false` — explicit invalidation only---# 🌐 12. CROSS-TAB SYNC ENFORCEMENT (ZERO-TRUST)Cursor MUST enforce all sub-rules below.---## RULE 12.1 — SINGLE CHANNELONE channel only: `BroadcastChannel('plans')`Singleton lives at: `@/lib/realtime/planChannel.js`❌ Forbidden:```javascriptnew BroadcastChannel('plans')   // outside planChannel.js```---## RULE 12.2 — ZERO-TRUST EVENTSEvents MUST carry type ONLY. NO data payload.❌ Forbidden:```javascriptpostMessage({ plans: [...] })postMessage({ planId: id })postMessage({ visibility: "public" })```✅ Required:```javascriptpostMessage({ type: 'PLAN_UPDATED' })postMessage({ type: 'PLAN_DEPRECATED' })```---## RULE 12.3 — PLANE SEPARATION- Platform Plane → EMIT only (`emitPlanUpdate`)- Public Plane → LISTEN only (`usePlanChannelListener`)- NO bidirectional channels---## RULE 12.4 — RECEIVER FLOWListeners MUST follow:```receive event → validate type → invalidateQueries → API refetch → render```❌ Forbidden:```javascriptsetPlans(event.data)   // direct state update from event```---## RULE 12.5 — MANDATORY CLEANUPAll listeners MUST clean up on unmount:```javascriptreturn () => channel.removeEventListener('message', handler);```❌ BLOCK IF MISSING---# 🔐 13. VISIBILITY ENFORCEMENT---## RULE 13.1 — BACKEND-ONLY FILTERINGPublic endpoints MUST filter at DB level:```javascript{ status: "active", visibility: "public" }```---## RULE 13.2 — NO FRONTEND FILTERING❌ Forbidden:```javascriptplans.filter(p => p.visibility === "public")```All filtering is server-side. Frontend renders what API returns.---## RULE 13.3 — LEAK ASSERTION REQUIREDAll public endpoints MUST include runtime assertion:```javascriptif (plans.some(p => p.visibility !== "public")) {  throw new Error("VISIBILITY_LEAK");}```---# 🔁 14. ENFORCED SYSTEM FLOWEvery Platform mutation MUST follow this exact sequence:```Platform Mutation (save / publish / deprecate / duplicate)        ↓DB updated (backend)        ↓queryClient.invalidateQueries(PLAN_QUERY_KEYS.ALL_PUBLIC)   ← local tab        ↓emitPlanUpdate({ type: 'PLAN_UPDATED' })                    ← other tabs        ↓[Other tabs] receive event → validate → invalidateQueries        ↓React Query auto-refetch → GET /public/plans        ↓UI renders updated data```Any step missing = BLOCK.---# 🚫 15. FORBIDDEN ACTIONSCursor MUST NEVER:- Break plane isolation- Bypass any security layer- Mix shared + tenant data- Modify architecture without spec- Generate partial implementations- Use frontend visibility filtering- Use setState for server data- Use manual refetch()- Send data payloads in BroadcastChannel events- Create multiple QueryClient instances- Skip cross-plane invalidation after mutations---# 🧠 16. SELF-VALIDATION ENGINEBefore ANY output, Cursor MUST verify:- Correct Plane? ✅- Tenant-safe? ✅- All 7 security layers present? ✅- Spec aligned? ✅- React Query rules followed? ✅- Cross-tab sync correct? ✅- Visibility enforced at backend? ✅- No hallucination? ✅If ANY fail:→ BLOCK---# 🚨 17. BLOCK RESPONSE FORMAT```❌ BLOCKED BY CURSOR RULES ENGINE v4.0Type: [Plane | Tenant | Security | ReactQuery | CrossTab | Visibility | Spec]Details: [exact violation]Required Fix: [action required]```---# ⚠️ 18. ESCALATION MODE```⚠️ CLARIFICATION REQUIREDMissing:[Plane / Schema / Rule / Key]Cannot proceed safely.```---# 🧬 19. VERSIONINGVersion: 4.0Mode: ENTERPRISE HARD ENFORCEMENTIsolation: ABSOLUTE + ZERO-TRUSTPlanes: Platform | Organization | Patient Portal | SupervisorCache: React Query — invalidation-driven, API-authoritativeSync: BroadcastChannel — zero-trust, type-only---# 🧾 FINAL LAWIf ANY output violates:- Plane Isolation- Tenant Isolation- Zero-Trust Security (any of 7 layers)- React Query Rules (11.1 – 11.6)- Cross-Tab Sync Rules (12.1 – 12.5)- Visibility Enforcement (13.1 – 13.3)- Spec-First Flow→ OUTPUT IS BLOCKED. NO EXCEPTIONS.